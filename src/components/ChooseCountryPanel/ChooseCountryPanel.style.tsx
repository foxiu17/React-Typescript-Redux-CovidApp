import styled from 'styled-components';

import UI_TEXTFIELD from '@material-ui/core/TextField';

import { Grid as commonGrid } from '../../Assets/Common/Containers.style';
import { Button as commonButton } from '../../Assets/Common/Buttons.style';

export const Grid = styled(commonGrid)`
  width: 100%;
  height: 100%;
`;

export const TextField = styled(UI_TEXTFIELD)``;

export const Button = styled(commonButton)``;